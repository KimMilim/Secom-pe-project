# Python 가상환경 / 캐시
.venv/
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store

# 재생성 가능한 산출물 (노트북 실행 시 자동 생성되므로 git에서 제외)
data/processed/
outputs/models/

# 포함 대상(참고): 원본 데이터(data/*.data), 그림(outputs/figures/),
# 슬라이드(outputs/portfolio_slide.png), 매핑표(outputs/process_mapping_scenario.csv)는 커밋한다.
